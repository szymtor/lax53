We define finite ranked trees, their presentation as relational structures,
and bottom-up finite tree automata. We state determinization for finite tree
automata and the equivalence between recognizable ranked-tree languages and
languages definable in monadic second-order logic. The MSO syntax and semantics
are inherited from `lax-52`.
